"""app.llm.prompts —— 供 Streamlit UI 调用的提示词模板。

所有模板均为 f-string 占位符形式，调用方负责填充。
数值指标由确定性 Python 工具计算，LLM 只负责解释与建议。
"""

GUARDRAIL_INTERPRETATION = """你是一名资深信用风险建模专家。请根据以下模型护栏结果，用中文给出 2-3 句话的简洁解释：
1. 模型是否可上线；
2. 若不可上线，最关键的一两个风险点是什么；
3. 建议的下一步动作。

护栏结果（JSON）：
{guardrail_json}

模型指标（JSON）：
{metrics_json}

要求：只给出结论与建议，不要编造任何未提供的数值。
"""

HITL_ADVICE = """你是一名信用风险建模专家，正在为人工复核员提供决策建议。

已知信息：
- 护栏结果：{guardrail_rationale}
- 未通过/警告项：{warnings}
- 测试集指标：{metrics_json}
- 建模步骤历史：{step_history}

请给出：
1. 如果批准上线，需要关注哪些风险；
2. 如果拒绝上线，建议优先调整哪些环节（数据、特征、模型参数等）；
3. 一句话结论。

要求：不要编造数值，仅基于提供的信息分析。
"""

VARIABLE_IMPORTANCE = """你是一名信用风险建模专家。以下是评分卡中各变量对模型的重要性排序（基于 WOE 与逻辑回归系数）。

请用中文简要说明：
1. 前 3 个最重要变量及其业务含义；
2. 是否有某个变量看起来反常或需要业务复核；
3. 一句话总结。

变量重要性（JSON）：
{importance_json}
"""

SCORE_EXPLANATION = """你是一名信用风险审批专家。请根据以下评分卡与申请人信息，用中文解释该笔申请的评分结果。

评分卡变量得分（JSON）：
{variable_scores_json}

最终评分：{total_score}
建议阈值：cutoff={cutoff}（≥ cutoff 建议通过，< cutoff 建议拒绝）

请解释：
1. 该申请是建议通过还是拒绝；
2. 哪几个变量对分数贡献最大（正面或负面）；
3. 一句话业务建议。

要求：只基于提供的变量得分做解释，不要编造其他信息。
"""
